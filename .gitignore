# Local HTTPS dev certificate (private key — never commit)
.certs/

# Node
node_modules/

# Logs / OS junk
*.log
.DS_Store
Thumbs.db

# Generated esbuild bundle (rebuild: npm run build:naa)
src/naa.bundle.js

# Deploy build folder (assembled by the deploy step)
dist/
